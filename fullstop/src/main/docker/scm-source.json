{
    "url": "${git.remote.origin.url}",
    "revision": "${git.commit.id.full}",
    "author": "${git.commit.user.name}",
    "status": "",
    "dirty": "${git.dirty}",
    "branch": "${git.branch}",
    "describe": "${git.commit.id.describe}"
}