spring:
  jpa:
    ## Toggle to print sql statements
    # show-sql: true
    hibernate:
      ddl-auto: create
    ## Toggle to print sql statements
    # properties:
      # hibernate.format_sql: true
